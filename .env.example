DATABASE_URL=postgresql+psycopg2://openclaw:password@postgis:5432/openclaw

# --- PREP / Auth ---
ADMIN_USERNAME=admin
ADMIN_PASSWORD=changeme

# --- Notifications / Costs ---
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
NOTIFY_EMAIL=
COST_SHORT_PLAT_BASE=8000
COST_ENGINEERING_PER_LOT=6000
COST_UTILITY_PER_LOT=12000
COST_BUILD_PER_SF=185
TARGET_HOME_SF=2200
LOB_API_KEY=

# --- Block A / Bundles ---
BUNDLE_ADJACENCY_TOLERANCE_FT=10
BUNDLE_SEARCH_RADIUS_FT=500
BUNDLE_SCORE_CAP=15
BUNDLE_CACHE_TTL_HOURS=24
BUNDLE_MAX_NEIGHBORS=50

# --- Block C / Voting ---
EDGE_WEIGHT_USER_UPVOTE=8
VOTE_NET_THRESHOLD=1

# --- Block D / Learning ---
LEARNING_WEIGHT_MAX_DELTA=15
LEARNING_WEIGHT_DECAY_HALFLIFE_DAYS=30
SCORE_STALENESS_THRESHOLD_HOURS=6
SPLIT_WEIGHT=40
VALUE_WEIGHT=25
OWNER_WEIGHT=15

# --- Block E / Enrichment ---
SKIP_TRACE_API_KEY=
SKIP_TRACE_ENABLED=false
SKIP_TRACE_RATE_LIMIT_PER_MIN=10
SKIP_TRACE_MAX_RETRIES=3
BUSINESS_FILINGS_ENABLED=false
ENRICHMENT_RETENTION_DAYS=365

# --- Block G / Reminders ---
REMINDER_CHECK_INTERVAL_MIN=5
REMINDER_EMAIL_ENABLED=false

# --- Block H / OSINT ---
OSINT_ENABLED=true
OSINT_BASE_URL=http://localhost:8000/api
OSINT_TIMEOUT_SECONDS=90
OSINT_BATCH_LIMIT=20
OSINT_BATCH_ENABLED=true
OSINT_UI_URL=

# --- General ---
EXPORT_MAX_ROWS=10000

# --- EDGE/RISK Tag System ---
# Minimum acreage for Lot Size Averaging tag (EDGE_SNOCO_LSA_R5_RD_FR)
EDGE_LSA_MIN_ACRES=10.0
# Minimum acreage for Rural Cluster bonus tag (EDGE_SNOCO_RURAL_CLUSTER_BONUS)
EDGE_RURAL_CLUSTER_MIN_ACRES=5.0
# Comma-separated zone codes where HB 1110 middle housing applies (leave blank = data unavailable)
EDGE_HB1110_URBAN_ZONES=
# Comma-separated zone codes supporting unit lot subdivision (leave blank = data unavailable)
EDGE_UNIT_LOT_ZONES=
# Score weight boosts for each EDGE tag
EDGE_WEIGHT_LSA=35
EDGE_WEIGHT_RUTA=30
EDGE_WEIGHT_HB1110=25
EDGE_WEIGHT_UNIT_LOT=20
EDGE_WEIGHT_RURAL_CLUSTER=15
EDGE_WEIGHT_BUNDLE_SAME_OWNER=10
EDGE_WEIGHT_BUNDLE_ADJACENT=5
# Per-RISK-tag score penalty (negative value, capped at -30 total)
EDGE_WEIGHT_RISK_PENALTY=-15
